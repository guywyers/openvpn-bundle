#define MOB_PRPOFILE_H
#include <stdio.h>
#include <stdbool.h>
#include "profile.h"

extern bool ToMobileProfile(FILE* from, FILE* to, profile_info * profile);

#ifndef MOB_PRPOFILE_H
#endif //!MOB_PRPOFILE_H